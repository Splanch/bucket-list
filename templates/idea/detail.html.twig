{% extends 'layout.html.twig' %}

{% block main %}
    <h2>{{ idea.title }}</h2>
    <p>{{ idea.description }}</p>
    <p>{{ idea.author }}, le {{ idea.dateCreated | date('d/m/Y')}}</p>
{% endblock %}

